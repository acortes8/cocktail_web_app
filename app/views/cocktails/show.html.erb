<h1><%= @cocktail.name %></h1>

<img src="<%= @cocktail.image_url %>" alt="Cocktail image" width="720" height="720">

<p><%= @cocktail.instruction %></p>

<ul>
  <% @cocktail_ingredients.each do |cocktail_ingredient| %>
    <li>
      <% if cocktail_ingredient.quantity.present? %>
        <%= cocktail_ingredient.quantity %>
      <% end %>

      <% if cocktail_ingredient.unit.present? %>
        <%= cocktail_ingredient.unit %>
      <% end %>

      <% if cocktail_ingredient.ingredient.name.present? %>
        <%= cocktail_ingredient.ingredient.name %>
      <% end %>

      <% if cocktail_ingredient.description.present? %>
        (<%= cocktail_ingredient.description %>)
      <% end %>
    </li>
  <% end %>
</ul>

<ul>
  <li><%= link_to "Edit", edit_cocktail_path(@cocktail) %></li>
  <li><%= link_to "Destroy", cocktail_path(@cocktail), data: {
    turbo_method: :delete,
    turbo_confirm: "Are you sure?"
  } %></li>
</ul>
