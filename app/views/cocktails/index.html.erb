<h1>Cocktails</h1>

<ul>
  <% @cocktails.each do |cocktail| %>
    <li>
      <a href="<%= cocktail_path(cocktail)%>">
        <%= cocktail.name %>
        <img src="<%= cocktail.image_url %>" alt="Cocktail image" width="100" height="100">
      </a>
    </li>
  <% end %>
</ul>
