<h1>Cocktails</h1>

<ul>
  <% @cocktails.each do |cocktail| %>
    <li>
      <%= link_to cocktail.name, cocktail %>
      <%= link_to image_tag(cocktail.image_url, size: 100), cocktail %>
    </li>
  <% end %>
</ul>

<%= link_to "New Cocktail", new_cocktail_path %>
